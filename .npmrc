# multer-storage-cloudinary@4 still declares a peerDep on cloudinary@^1,
# but works fine with cloudinary v2 (which dropped the vulnerable vm2/ip chain).
# This keeps `npm install` from failing on that stale peer conflict during deploys.
legacy-peer-deps=true
